name: Run Tests

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code
      - name: Checkout repository
        uses: actions/checkout@v3

      # Setup .NET
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0' # Adjust to your project's .NET version

      # Restore dependencies
      - name: Restore dependencies
        run: |
         dotnet restore ./MTOGO.UnitTests/MTOGO.UnitTests.csproj
         dotnet restore ./MTOGO.IntegrationTests/MTOGO.IntegrationTests.csproj



      # Build the solution
      - name: Build solution
        run: dotnet build --no-restore --configuration Release

      # Run Unit Tests
      - name: Run Unit Tests
        run: dotnet test ./MTOGO.UnitTests/MTOGO.UnitTests.csproj --no-build --configuration Release

      # Run Integration Tests
      - name: Run Integration Tests
        run: dotnet test ./MTOGO.IntegrationTests/MTOGO.IntegrationTests.csproj --no-build --configuration Release
